import { FixtureWrapper } from "FixtureWrapper";
import { ToggleMode } from "./ToggleMode";

export default {
  "base case": (
    <FixtureWrapper>
      <ToggleMode mode="Notes" />
    </FixtureWrapper>
  ),
};
